<?xml version="1.0" encoding="utf-8" ?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    
    xmlns:app="http://schemas.android.com/apk/res-auto">
<item
    android:id="@+id/action_search"
    android:title="#caja"
    android:icon="@drawable/ic_search_black_24dp"
    app:showAsAction="always"
    app:actionViewClass="android.support.v7.widget.SearchView"
  />
    <item
        android:id="@+id/action_order"
        android:title="Ordenamiento..."
        android:icon="@drawable/ic_back"
        app:showAsAction="never">

        <item
            android:id="@+id/action_or_container"
            android:title="Ordenar por Nombre"
            android:icon="@drawable/ic_back"
            app:showAsAction="never"/>
        <item
            android:id="@+id/action_or_status"
            android:title="Ordenar por estatus"
            android:icon="@drawable/ic_back"
            app:showAsAction="never"/>
        <item
            android:id="@+id/action_or_date"
            android:title="Ordenar por fecha"
            android:icon="@drawable/ic_back"
            app:showAsAction="never"/>

    </item>
</menu>