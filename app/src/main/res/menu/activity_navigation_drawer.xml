<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:title="@string/app_name">
            <menu>
                <item
                    android:id="@+id/nav_perfil"
                    android:icon="@drawable/ic_perfil"
                    android:visible="false"
                    android:title="Perfil" />
                <item
                    android:id="@+id/nav_list"
                    android:icon="@drawable/ic_list"
                    android:title="Lista" />
            </menu>
        </item>
    </group>

    <item android:title="Utilitarios">
        <menu>
            <item
                android:id="@+id/nav_tools"
                android:icon="@drawable/ic_menu_manage"
                android:title="Configuración" />

            <item
                android:id="@+id/nav_delDB"
                android:icon="@drawable/ic_delete_db"
                android:title="Borrar Todo" />

            <item
                android:id="@+id/nav_xxx"
                android:icon="@drawable/ic_menu_send"
                android:visible="false"
                android:title="modificar" />
        </menu>

    </item>

    <item android:title="Sesión">
        <menu>
            <item
                android:id="@+id/nav_exit"
                android:icon="@drawable/ic_exit"
                android:title="Salir" />
        </menu>
    </item>

</menu>
